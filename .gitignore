node_modules/
dist/
.astro/
.DS_Store
.env*
.claude/worktrees/
test-results/
playwright-report/
.lighthouseci/

# AI & Knowledge Graph Local Artifacts
.obsidian/
graphify-out/
.codegraph/
# The Obsidian vault graphify writes. Derived from `src/`, so versioning it would
# version a cache: it goes stale the first time an import changes and nobody re-runs
# graphify, and then the repo asserts something false. `CLAUDE.md` names the command.
docs/architecture/

# LinkedIn banner — derived artifacts, regenerate with assets/linkedin/gen-banner.py
assets/linkedin/*.png
assets/linkedin/*.html
